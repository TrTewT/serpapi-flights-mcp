services:
  - type: web
    name: serpapi-flights-mcp
    runtime: python
    buildCommand: pip install -e .
    startCommand: serpapi-flights
    envVars:
      - key: SERPAPI_API_KEY
        sync: false
      - key: MCP_TRANSPORT
        value: http
      - key: MCP_HOST
        value: 0.0.0.0
